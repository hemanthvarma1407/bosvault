{
  "name": "@org/frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/frontend/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx next build && npx rimraf ../../dist/frontend && node -e \"const fs=require('fs'); fs.cpSync('out', '../../dist/frontend', {recursive:true});\"",
        "cwd": "packages/frontend"
      },
      "outputs": [
        "{workspaceRoot}/dist/frontend"
      ]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx next dev",
        "cwd": "packages/frontend"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx next start",
        "cwd": "packages/frontend"
      }
    }
  }
}